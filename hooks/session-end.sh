#!/bin/bash
YELLOW='\033[0;33m'
DIM='\033[2m'
RESET='\033[0m'

echo -e "${YELLOW}Memory${RESET} ${DIM}|${RESET} Session ending — if you haven't already offered a retro this session, ask the user now: 'Anything worth storing before we close?'"
